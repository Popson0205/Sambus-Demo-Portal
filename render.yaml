services:
  - type: web
    name: sambus-demo-portal
    env: node
    buildCommand: bun install && bun run build
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
